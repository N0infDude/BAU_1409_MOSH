import mathP=math.pi
a=[[1,4],[1,4],[1,4],[1,4]]for i in range(len(a)):
    Glast=8    k1=a[i][1]/a[i][0]
    for j in range(a[i][0]):        k=k1/Glast
    def E(T):            summ = 0
        for i in range(T):                summ += i + 1
        return summ        def M():
        192 + Gnew  # масса        def V():
            return 2 * ((80 - E / 11) / 80) * (200 / M)  # скорость        def Gnew():
            return Glast + Glast * K  # кол-во цветов сейчас        def K():
            return math.sin((-1 * (P) / 2) + (P * (T + 0, 5 * O) / 40))